[build]
  command = "npm run build"
  publish = "dist"

# Redirects for React Router (Single Page Application)
[[redirects]]
  from = "/travel-prep/*"
  to = "/travel-prep/index.html"
  status = 200

# Fallback for root (optional, but good practice if needed)
# [[redirects]]
#   from = "/*"
#   to = "/index.html"
#   status = 200
